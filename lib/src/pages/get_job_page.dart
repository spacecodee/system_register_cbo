import 'package:flutter/material.dart';

class GetJobPage extends StatelessWidget {
  const GetJobPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Get Job Page'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
